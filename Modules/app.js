import {add} from './mathUtils.js';
import multiply from './mathUtils.js';

// Default Export: You can only have one default export per module. It can be either a function, class, or object, 
// but not multiple default exports.
// Named Exports: If you want to export multiple functions or variables, use named exports instead of default.
// Importing Default Export: You don't need to use curly braces when importing a default export.

add(7, 8);
multiply(8, 3);